  - sensor:
      - unique_id: black_tank_percent
        name: "Black Tank Percent"
        unit_of_measurement: '%'
        icon: "mdi:gauge"
        state: >
          {% set t = 7.0 %}
          {{ ((states("sensor.propane_sensor_tank_level") | float(default=0.0)) / t * 100.0) | round() }}
